kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: allow-lab-only
  namespace: testing
spec:
  podSelector:
    matchLabels:
      lab: initial
  ingress:
    - from:
      - podSelector:
          matchLabels: 
            lab: initial